// import * as React from "react";

import { Layout } from "../components/Layout";

// type User = {
//   name: string;
// };

export function User(props: {}) {
  return (
    <Layout>
      <ul>
       test user
      </ul>
    </Layout>
  );
}
